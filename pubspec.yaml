name: hotel_cartagenena_mod_reservas
description: Sistema de gestión de reservas del Hotel Cartagena (versión modular).
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  test: ^1.24.0
